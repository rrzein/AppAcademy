<% flash[:errors].try(:each) do |msg| %>
<%= msg %>
<% end %>

<h1>Sign In<h1>
	<form action="<%= session_url %>" method="post">

		<label for="user_username">Username</label>
		<input type="text" name="user[username]" id="user_username">
		<br>
		<label for="user_password">Password</label>
		<input type="password" name="user[password]" id="user_password">
		<br>
		<input type="submit" value="Sign In">
	</form>

	<!-- you would have passed the last auth_spec spec if you just included "sign-in" as a link -->
	<!-- should have included "SIGN IN" link in the LAYOUT form, so it's included in ALL pages. JUST COVER IT FOR EVERYTHING. -->

	<!-- include in VIEWS/LAYOUTS/APPLICATION.HTML.ERB the sign-in link and sign-out buttons -->